jobs:
  build:
    docker:
      - image: cimg/node:lts
    working_directory: ~/repo
    steps:
      - circleci-queue/until_front_of_line:
          # non-defaults for each paramter
          time: "1"
          consider-branch: false
          dont-quit: true
          block-workflow: true
